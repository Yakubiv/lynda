.navbar-header
  %a.navbar-brand{href: appointments_path} BeautySpa
#navbar
  %ul.nav.navbar-nav.navbar-right
    - if admin_signed_in?
      %li= link_to(fa_icon('power-off'), destroy_admin_session_path, method: :delete)
